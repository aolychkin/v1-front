import Sheet from "@mui/joy/Sheet";
import Typography from "@mui/joy/Typography";
import { FieldSizeError } from "./field-size-error";
// import { TActionField, TFieldConfig } from "pages/boards/model";
// // import { TCardField } from "pages/boards/model/types";


// export const CardSlotInlineText = ({ size, field, config }: { size: number; field?: TActionField, config?: TFieldConfig }) => {
//   console.log("CONFIG", config)

//   if (size === 12) {
//     return (
//       <Sheet sx={{ width: size / 12 }}>
//         <Typography level='title-sm'>{field?.value}</Typography>
//       </Sheet>
//     )
//   } else {
//     return (
//       <FieldSizeError />
//     )
//   }
// }